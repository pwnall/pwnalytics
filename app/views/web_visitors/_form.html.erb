<%= form_for(@web_visitor) do |f| %>
  <% if @web_visitor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@web_visitor.errors.count, "error") %> prohibited this web_visitor from being saved:</h2>

      <ul>
      <% @web_visitor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :web_property_id %><br />
    <%= f.text_field :web_property_id %>
  </div>
  <div class="field">
    <%= f.label :uid %><br />
    <%= f.text_field :uid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
