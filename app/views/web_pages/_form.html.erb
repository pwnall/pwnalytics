<%= form_for(@web_page) do |f| %>
  <% if @web_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@web_page.errors.count, "error") %> prohibited this web_page from being saved:</h2>

      <ul>
      <% @web_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :web_property_id %><br />
    <%= f.text_field :web_property_id %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
