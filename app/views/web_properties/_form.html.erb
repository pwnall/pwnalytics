<%= form_for(@web_property) do |f| %>
  <% if @web_property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@web_property.errors.count, "error") %> prohibited this web_property from being saved:</h2>

      <ul>
      <% @web_property.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
